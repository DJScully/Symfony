{% extends 'base.html.twig' %}

{% block title %} <title>Editoriales</title>  {% endblock %}
{% block body %}  
{{ include('menu.html.twig', { selectedMenu: 'home'}) }}
<br>
<a class="btn btn-primary" href="{{ path('editorial-new') }}">Crear Nueva Editorial</a>
<br><br>
<table class="table table-dark table-bordered table-striped">
  <thead>
    <th>ID</th>
    <th>Nombre</th>
    <th>Ver</th>
    <th>Actualizar</th>
    <th>Eliminar</th>
  </thead>
  <tbody>
  {% for autor in fondos %} 
    <tr>
        <td> {{autor.id}} </td>
        <td> {{ autor.nombre }} </td>
        <td> <a  class="btn btn-info" href="{{ path('editorial-ver-uno', {id: autor.id}) }}">Ver</a> </td>
        <td> <a  class="btn btn-secondary" href="{{ path('editorial-act', {id: autor.id}) }}">Actualizar</a> </td>
        <td> <a  class="btn btn-outline-danger" href="{{ path('editorial-delete', {id: autor.id}) }}">Eliminar</a> </td>
     </tr>

  {% endfor %}
  </tbody>
   
</table>


{% endblock %}