{% extends 'base.html.twig' %}

{% block title %} <title>Vista</title>  {% endblock %}
{% block body %}  
{{ include('menu.html.twig', { selectedMenu: 'home'}) }}
<br>
<a class="btn btn-primary" href="{{ path('editorial-new') }}">Crear Nueva Editorial</a>
<br><br>
<table class="table table-dark table-bordered table-striped">
  <thead>
    <th>Nombre</th>
    <th>Ver</th>
    <th>Actualizar</th>
    <th>Eliminar</th>
  </thead>
  <tbody>
  {% for autor in fondos %} 
    <tr>
        <td> {{ autor.nombre }} </td>
        <td> <a  class="btn btn-primary" href="{{ path('editorial-ver-uno', {id: autor.id}) }}">Ver</a> </td>
        <td> <a  class="btn btn-primary" href="{{ path('editorial-act', {id: autor.id}) }}">Actualizar</a> </td>
        <td> <a  class="btn btn-primary" href="{{ path('editorial-delete', {id: autor.id}) }}">Eliminar</a> </td>
     </tr>

  {% endfor %}
  </tbody>
   
</table>


{% endblock %}