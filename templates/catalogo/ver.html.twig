{% extends 'base.html.twig' %}

{% block title %} <title>Vista</title>  {% endblock %}
{% block body %}  
{{ include('menu.html.twig', { selectedMenu: 'home'}) }}
<br>
<br><br>
 <table class="table table-dark table-bordered table-striped">

      
            <thead>
                <th>Titulo</th>
                <th>Autor</th>
                <th>Editorial</th>
                <th>Edicion</th>
                <th>Publicacion</th>
                <th>Categoria</th>
               
            <thead>
            <tbody>
                
                   
                        <tr>
                            <td> {{fondo.titulo}} </td>
                            <td> {{fondo.autor[0].nombre }} </td>
                            <td> {{fondo.editorial.nombre}} </td>
                            <td> {{fondo.edicion}} </td>
                            <td> {{fondo.publicacion}} </td>
                            <td> {{fondo.categoria}} </td>
                        </tr> 
                   
                  
            </tbody>
        </table>

    <a  class="btn btn-primary" href="{{ path('catalogo') }}"  >Volver al listado</a>
    <a  class="btn btn-primary" href="{{ path('catalogo-act', {id: fondo.id}) }}">Actualizar</a>
    <a  class="btn btn-primary" href="{{ path('catalogo-delete', {id: fondo.id}) }}">Eliminar</a>


{% endblock %}