{% extends 'base.html.twig' %}

{% block title %} <title>Lista catálogo</title>  {% endblock %}
{% block body %}  

    
      

       {{ include('menu.html.twig', { selectedMenu: 'home'}) }}

<a class="btn btn-primary" href="{{ path('catalogo-new') }}">Crear Nueva Editorial</a>
        <table class="table table-dark table-bordered table-striped">

      
            <thead>
                <th>Titulo</th>
                <th>Autor</th>
                <th>Editorial</th>
                <th>Edicion</th>
                <th>Publicacion</th>
                <th>Categoria</th>
                <th>Ver</th>
                <th>Actualizar</th>
                <th>Eliminar</th>
            <thead>
            <tbody>
                
                    {% for fondo in fondos %}
                        <tr>
                            <td> {{fondo.titulo}} </td>
                            <td> {{fondo.autor[0].nombre }} </td>
                            <td> {{fondo.editorial.nombre}} </td>
                            <td> {{fondo.edicion}} </td>
                            <td> {{fondo.publicacion}} </td>
                            <td> {{fondo.categoria}} </td>
                            <td> <a  class="btn btn-primary" href="{{ path('catalogo-ver', {id: fondo.id}) }}">Ver</a> </td>
                            <td> <a  class="btn btn-primary" href="{{ path('catalogo-act', {id: fondo.id}) }}">Actualizar</a> </td>
                            <td> <a  class="btn btn-primary" href="{{ path('catalogo-delete', {id: fondo.id}) }}">Eliminar</a> </td>
                        </tr> 
                    {% endfor %}
                  
            </tbody>
        </table>
       
      
{% endblock %}